//leetcode 136. Single Number

#include<iostream>
#include <vector>
using namespace std;

class Solution {
public:
    int singleNumber(vector<int>& nums) { // ampersand & used in front of nums to make it pass by reference (containers like vectors are pass by value by default) so an alias is created by the name of &nums for nums vector
        
    }
};